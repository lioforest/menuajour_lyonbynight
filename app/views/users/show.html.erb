<div class="container">

	<div class="d-flex align-items-center align-content-around mt-5">

		<div class="col-md-8 d-flex flex-column align-items-center justify-content-around">
			
			<span><%= @user.first_name%> <%= @user.last_name%></span>
			<br/>
			<p><%= @user.email%></p>
			<br/>
			<a href="<%= edit_user_registration_path %>" class="btn btn-primary">Edit my personal information</a>

		</div>

			<a href="<%= user_menus_path(current_user.id) %>" class="btn btn-primary">Voir mes menus</a>
			<br/>
			<a href="#" class="btn btn-primary">Voir mes plats</a>
			
		</div>


	</div>

</div>